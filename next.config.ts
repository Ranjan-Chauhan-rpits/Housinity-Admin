import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['via.placeholder.com', 'thumbs.dreamstime.com', 't3.ftcdn.net'],
  },
};

export default nextConfig;
